# W401 FIX337｜新增員工與 GitHub 打卡帳號操作說明

## 這次修正的原因

W400 FIX336 回歸單機模式時，舊的雲端頁面清理規則誤把「新增／編輯員工」頁面一起隱藏；同時舊主系統的員工登入帳號／TOKEN 與新版 GitHub 打卡所需的「員工編號＋PIN」沒有整合，因此主管找不到新增員工入口，也無法知道員工要用哪一組 PIN。

W401 FIX337 已把兩個問題一起修正。

## 主管新增員工

1. 開啟單機主系統並登入管理者。
2. 進入「工作・人事・薪資整合」。
3. 進入「員工管理中心」。
4. 按「＋ 新增員工」。
5. 填寫員工姓名、員工編號、部門／工作區、狀態與備註。
6. 系統會自動產生 6 位數 GitHub 打卡 PIN，也可以按「重新產生」。
7. 按「儲存／產生」。

## 主管查看員工編號與 PIN

回到「員工管理中心 → GitHub 定位打卡帳號」。

每位員工會顯示：
- 姓名
- 員工編號
- 部門
- 6 位打卡 PIN（預設遮蔽，可按顯示）
- 可否登入打卡
- 複製登入資料
- 重設 PIN
- 編輯員工
- 詳細資料

按「複製登入資料」後，可直接把該員工的「員工編號＋PIN」傳給本人。

## 同步到 GitHub 打卡驗證端

1. 先到「出勤管理中心 → GitHub 員工打卡橋接」。
2. 填入 Apps Script Web App `/exec` 網址。
3. 填入 `SYNC_KEY`。
4. 啟用外部打卡同步並儲存。
5. 回到「員工管理中心」。
6. 按「同步打卡帳號至 Apps Script」。

新增員工、停用員工或重設 PIN 後，都應再按一次同步，讓 GitHub 打卡端立即使用最新帳號狀態。

## 員工手機登入

員工打開 GitHub Pages 打卡網址後，輸入：
- 員工編號：主系統建立的員工編號
- PIN：主系統建立的 6 位打卡 PIN

GitHub 原始碼內不保存員工 PIN，也不再內建 E001 / 123456 之類的預設正式登入帳號。

## 舊帳號與打卡 PIN 的差別

主系統過去的「員工自助登入帳號／密碼／TOKEN」屬於舊的人事自助入口；GitHub 手機打卡只使用「員工編號＋6 位打卡 PIN」。兩者用途不同，W401 FIX337 已在畫面上明確分開，避免混淆。
