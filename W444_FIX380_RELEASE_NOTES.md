# W444 FIX380｜員工請假操作與工作資料連動

日期：2026-09-06

## 本版重點

- 修正員工端「送出休假日期調整申請」按下後沒有可見回饋的問題；送出中、成功、失敗都會顯示在同一張卡片。
- 有明確剩餘額度且已為 0 的假別，在「新增請假」時直接禁用，選單標示「剩餘 0 日｜不可申請」。
- 修改既有待審請假時，若原申請本身已占用完該假別額度，仍允許保留原假別進行修正，避免無法編輯原申請。
- 開始日期變更時，結束日期自動同步到新開始日；結束日期最小值同步鎖定，不允許早於開始日期。
- 事假、照顧家庭成員事假、家庭照顧假共用額度採一致計算；家庭照顧假同時受自身與共用上限控制。
- 主系統新增員工端工作資料同步診斷，可看到工作項目表、個人工作任務、待辦工作任務與薪資快照筆數。
- 生產批次事件月曆新增「同步工作到員工端」按鈕，可在重新產生月曆後直接推送部門工作項目。
- 員工端工作項目表／我的工作任務無資料時，直接提示主管應從主系統哪裡產生、指派與同步。
- 薪資排班審核完成並同步時，會再嘗試同步工作資料，降低班表已發布但工作任務仍空白的情況。

## 工作資料來源

### 工作項目表
主系統：`生產批次事件月曆 → 重新產生月曆 → 同步工作到員工端`。

這是部門／全場層級工作，員工端「工作項目表」不要求先指派個人。

### 我的工作任務
主系統：`生產批次事件月曆 → 全場工作與人力排程 → 人力配置與排班橋接 → 指定主責／支援員工 → 儲存配置 → 審核／核准並同步`。

只有正式指派給該員工的工作，才會出現在員工端「我的工作任務」。

## 相容性

- Apps Script `Code.gs` schema 沿用 **W441 FIX377 CLEAN**，本版不需要重新部署 Apps Script。
- GitHub 員工端必須更新至 **W444 FIX380**。
- 正式主系統建議使用 DATA-SAFE 更新包；不要用 CLEAN 完整包覆蓋既有 `data/`。
