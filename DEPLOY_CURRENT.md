# CURRENT DEPLOYMENT

- Employee portal: **W456 FIX392-R2**
- Apps Script protocol: **W456_FIX392_CLEAN**
- Login: **login → portalData**; health is diagnostics only.
- Current saved device `/exec` has priority over bundled config.js.

If the screen does not show `W456 FIX392-R2`, the browser/PWA is still using an older GitHub build.
