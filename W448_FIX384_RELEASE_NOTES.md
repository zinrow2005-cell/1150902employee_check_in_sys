# W448 FIX384｜生產批次事件月曆 runtime 修復＋員工端殘留清理

日期：2026-09-07

本版延續 **批次工作單一中心** 架構。

## 修復重點

1. 修復「生產批次事件月曆」在 W447 簡化全場工作排程後，前端 `load()`、工作明細、工作量分數與排休顯示 helper 被一併移除，導致頁面初始化在 `closeWorkDetail is not defined`／`load is not defined` 前中斷，畫面永遠停留在「正在產生生產批次事件月曆」。
2. 重新產生月曆按鈕加入明確的「正在產生 → 完成／失敗」狀態。API 回傳錯誤或資料格式不完整時會在月曆區顯示原因，不再無限等待。
3. 恢復批次工作卡片的工作量分數、工作明細、執行步驟、檢查表、工具／物資與相關系統顯示，但不恢復已退場的人力配置／個人派工功能。
4. 員工 GitHub 端移除 W447 退場「我的工作任務」後殘留的 `taskFilter → renderTasks` 事件監聽。原本該元件與函式已不存在，會造成員工端 JavaScript 初始化中斷；W448 已清乾淨。
5. W447 的「批次工作月曆為唯一重大工作中心」方向不變。全場工作排程、人力配置橋接、個人工作任務與完成回報仍保持退場。

## 其他頁面檢測

- 主系統 `farm_main_system/tools/check_all.py` 全部通過。
- 110 個主系統／員工端／飼料／氣象等 HTML 檔進行結構與前端稽核；73 個可直接檢查的 inline JavaScript 區塊語法 0 錯誤。
- 59 個獨立 JavaScript 檔執行 `node --check`，0 錯誤。
- 以登入管理者身分巡檢 316 個無 path parameter 的 GET 頁面／入口：除線圖／市場圖表模組在測試容器未安裝 `sqlmodel` 而回傳預期 503 外，未發現新的 5xx。正式 RUN 啟動器已將 `sqlmodel` 列為必要套件，缺少時會先安裝／阻擋啟動，因此不屬於本次程式回歸。

## Apps Script

Apps Script bridge schema 沿用 W441 FIX377；本版不需重新部署 `Code.gs`。
