# W441 FIX377｜版本殘留清理與資料連動驗證版

- 清除主系統與員工端會混淆部署的 W439/W440 舊版發行文件。
- 清除薪資頁面中已被新版本覆寫、仍引用舊全勤欄位的死程式碼；正式薪資 UI 核心函式各保留一份。
- 修正版本清理器：不再誤刪當前發行說明，並會移除舊版發行文件。
- 新增伺服器端薪資設定正規化。舊 `fullAttendanceBonus` 只會保薪轉入固定薪資一次，舊快取頁面回寫也不會把全勤獎金重新帶回正式制度。
- 薪資完整性檢查與正式加班費使用相同的固定工資納入旗標。
- 驗證 DATA-SAFE 更新不含 `data/`，主系統薪資快照可完整送入員工端橋接，請假／班表資料不會因局部儲存被覆蓋。
- 歷史已鎖定薪資單保留 `fullBonus` 只作歷史顯示，不重新計算。
